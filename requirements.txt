# Core
python-dotenv>=1.0.0
pydantic>=2.0.0
requests>=2.31.0
colorama>=0.4.6
tabulate>=0.9.0

# LLM providers
openai>=1.40.0
groq>=0.9.0

# RAG / Documents
pypdf>=4.0.0
# Optional better search later: chromadb, sentence-transformers

# Computer Vision (AI Camera Brain)
opencv-python>=4.8.0
pillow>=10.0.0
numpy>=1.24.0

# Voice (optional, for desktop/Termux with mic)
SpeechRecognition>=3.10.0
pyttsx3>=2.90
# Next level voice (install when ready):
# openai-whisper
# piper-tts

# Optional utilities
rich>=13.0.0

# Realtime bridge (optional)
# websockets>=12.0
# fastapi>=0.110.0
# uvicorn>=0.27.0

# Minecraft companion (Node.js side — see minecraft/README.md)
# Requires Node.js + npm install inside minecraft/
